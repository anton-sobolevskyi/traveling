import { PlacePhotoPipe } from './place-photo-pipe';

describe('PlacePhotoPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacePhotoPipe();
    expect(pipe).toBeTruthy();
  });
});
